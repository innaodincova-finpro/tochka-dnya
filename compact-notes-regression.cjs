const {JSDOM}=require('jsdom'),fs=require('fs'),assert=require('assert');
const html=fs.readFileSync('index.html','utf8');const dom=new JSDOM(html,{runScripts:'dangerously',url:'https://test.invalid/',beforeParse(w){w.matchMedia=()=>({matches:false,addListener(){}});w.scrollTo=()=>{};w.alert=()=>{};}});
setTimeout(()=>{const w=dom.window;const text='Подключаем первого пользователя. Далее идёт длинное пояснение, которое должно быть обычным фрагментом.\n\n1. Первый пункт\n2. Второй пункт';w.eval(`S=blank();S.settings.onboarded=1;S.notes=[{id:'dense',date:today(),kind:'note',text:${JSON.stringify(text)}}];renderAll();setNotesTab('note');`);const b=w.document.querySelector('.compact-note .note-open');assert(b);assert.equal(b.querySelector('.note-list-title').textContent,'Подключаем первого пользователя.');assert(b.querySelector('.note-list-preview').textContent.includes('Далее идёт'));assert(!b.querySelector('.note-card-open'));b.click();const body=w.document.querySelector('.note-reader-body');for(const line of text.split('\n').filter(Boolean))assert(body.textContent.includes(line));assert.equal(w.eval('S.notes[0].text'),text);assert(w.eval("noteListCopy('Рецепт аджики').preview")==='');assert.equal(w.eval("noteListCopy('Очень длинное название без знаков препинания').title"),'Очень длинное название без знаков препинания');console.log('PASS: title/preview, short note, no data changes, full reading by row click');dom.window.close();},200);
