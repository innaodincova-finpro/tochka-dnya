const {JSDOM}=require('jsdom'),fs=require('fs'),assert=require('assert');
const text='Для вас, Инна: как отправить приглашение\n\n1. Откройте «Ещё» → «Кабинет».\n2. Укажите почту человека и нажмите «Создать ссылку».\n3. Нажмите «Поделиться», выберите мессенджер и получателя, отправьте сообщение.\n4. Отправьте следом инструкцию для пользователя выше.\n5. После его входа нажмите в кабинете «Обновить».';
const dom=new JSDOM(fs.readFileSync('index.html','utf8'),{runScripts:'dangerously',url:'https://test.invalid',beforeParse(w){w.matchMedia=()=>({matches:false,addListener(){}});w.scrollTo=()=>{};w.alert=()=>{};w.confirm=()=>true;}});
setTimeout(()=>{const w=dom.window;w.eval('S=blank();S.settings.onboarded=1;');w.eval(`openSheet('voice');document.getElementById('f-speech').value=${JSON.stringify(text)};doParse();`);assert.equal(w.eval('S.notes.length'),0);w.eval('applyParse();openNote(S.notes[0].id)');const body=w.document.querySelector('.note-reader-body');assert.equal(w.eval('S.notes.length'),1);assert.equal(w.eval('S.notes[0].text'),text);assert.equal(body.querySelectorAll('.nt-num').length,5);for(const line of text.split('\n').filter(Boolean))assert(body.textContent.includes(line));console.log('PASS: paste -> preview -> save -> open; exact text saved, five complete rendered steps');dom.window.close();},200);
